---
description: Create a GTM video package by delegating to the canonical swe-gtm-video skill.
argument-hint: [feature, PRD/spec/story path, launch brief, or demo target]
disable-model-invocation: true
---

Read `.llm/references/SKILL-PROXY-RULES.md` or `~/.llm/references/SKILL-PROXY-RULES.md`, using the first one that exists, and use it to execute the canonical skill named `swe-gtm-video`.

Primary repo path: `swe-gtm-video/SKILL.md`

If arguments are present, treat them as the user's GTM video request:

`$ARGUMENTS`
